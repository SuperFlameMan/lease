<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.lease.web.app.mapper.ViewAppointmentMapper">
    <resultMap id="listItemResultMap" type="com.atguigu.lease.web.app.vo.appointment.AppointmentItemVo"
               autoMapping="true">
        <id property="id" column="id"/>
        <collection property="graphVoList" ofType="com.atguigu.lease.web.app.vo.graph.GraphVo" select="graphInfoSelect"
                    column="apartment_id" autoMapping="true"/>
    </resultMap>
    <select id="listItem" resultMap="listItemResultMap">
        select va.id,
               va.appointment_time,
               va.appointment_status,
               ai.name apartment_name,
               ai.id   apartment_id
        from view_appointment va
                 left join apartment_info ai on va.apartment_id = ai.id and ai.is_deleted = 0
        where va.is_deleted = 0
          and va.user_id = #{id}
        order by va.appointment_time desc

    </select>
    <select id="graphInfoSelect" resultType="com.atguigu.lease.web.app.vo.graph.GraphVo">
        select id,
               name,
               url
        from graph_info
        where is_deleted = 0
          and item_id = #{apartment_id}
          and item_type = 1
    </select>
</mapper>
